import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    mainContainer: {
        flex: 1,
    },
    imageBackground: {
        flex: 1,
        backgroundColor: 'red',
    }
})

export default styles